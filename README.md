# WISHME frontend

React + TypeScript + Vite app for WISHME.

```bash
npm install
npm run dev
```

The Laravel API lives in `../api-wishme`. Frontend and backend are not connected yet.
"# wishme-react" 
